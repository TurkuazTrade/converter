# Infra

Placeholder for deployment profiles, reverse proxy config, environment templates, and production compose files.

The root `docker-compose.yml` remains the default local development entrypoint.
